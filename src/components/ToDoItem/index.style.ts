import { createStyles } from "@mantine/core";

const useStyles = createStyles((theme) => ({
  contentBox: {
    width: "70%",
  },
  content: {
    padding: 2,
  },
  actionIcons: {
    width: "20%",
    textAlign: "center",
  },
  actionIcon: {
    margin: "auto",
  },
}));

export default useStyles;
